/**
 * Lotus Size Chart Initializer
 * This file only initializes the size calculator and serves as an entry point.
 * All actual functionality is contained within the isolated IIFE in the _size-chart.html partial.
 */

document.addEventListener('DOMContentLoaded', function() {
    console.log('Size Chart Main Initializer: Ready');
    
    // The core size calculator functionality is completely contained
    // within the _size-chart.html file in an isolated IIFE context
    
    // This file exists mainly as an entry point and to handle any potential
    // future cross-component interactions if needed
});