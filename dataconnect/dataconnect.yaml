specVersion: "v1"
serviceId: "lotus"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "lotus-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
